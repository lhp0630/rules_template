---
description: 规划与架构任务时始终包含
alwaysApply: true
---

每次 Plan/Architect 任务开始前, MUST 完成以下 3 项:
- 阅读 `docs/` 中的现有文档: i. `docs/architecture.md`, ii. `docs/product_requirement_docs.md`, iii. `docs/technical.md`
- 阅读 `tasks/` 中的计划及相关任务规划与上下文: i. `tasks/active_context.md`, ii. `tasks/tasks_plan.md`
- 从 `src` 及项目中其他代码文件获取所需方案上下文。

---

## 规划工作流

1. 理解需求:
   - 始终主动追问澄清与补充信息。
   - 识别描述不清的需求, 并索取详细说明。
   - 全面理解问题的各个方面, 收集细节, 使需求精确、清晰。
   - 针对所有需要做出的假设与推断进行提问, 消除歧义与不确定性。
   - 提出用户未想到的方案, 即预判需求与待明确事项。
   - 仅在百分之百清晰、有把握时, 再进入方案阶段。

2. 制定方案:
   - 为方案建立 meta 架构规划。
   - 将问题拆解为关键概念与更小的子问题。
   - 思考所有可能的解决路径。
   - 设定评估标准与权衡, 以衡量各方案的优劣。
   - 找出最优方案, 明确其最优依据及所涉及的权衡。
   - 必要时检索网络进行调研。
   - 严格论证方案的最优性。
   - 质疑每一个假设与推断, 并用充分推理支撑。
   - 思考能否优于当前方案, 综合不同方案的最强优点。
   - 重复上述流程, refine 并整合不同方案, 直至得到稳健方案。

3. 方案验证:
   - 尽可能详细地给出 PLAN。
   - 逐步拆解方案, 逐步想清楚每一步。
   - 论证其相对其他有力方案的最优性。
   - 明确说明所有假设、选择与决策。
   - 解释方案中的权衡。
   - 必要时, 在给出方案后用你自己的话重述用户的问题。
   - 在实现之前, 验证方案。

---

## 方案应具备的特征:

1. 方案应当:
   a. `extendable` (可扩展): 后续代码可轻松基于当前规划构建; 未来扩展应得到良好支持; 预判未来功能, 使规划可适配。
   b. `detailed` (详尽): 方案应非常详细, 覆盖所有受影响方面及各种可能情况。
   c. `robust` (稳健): 规划错误场景与失败情况, 并为可能的失败准备降级/兜底。
   d. `accurate` (准确): 各方面应彼此一致; 各组件应正确, 接口应正确。

---

每次 Plan/Architect 任务结束后, MUST 完成以下 2 项:
- 将方案写入现有文档并更新 `docs/` 中的文件: i. `docs/architecture.md`, ii. `docs/product_requirement_docs.md`, iii. `docs/technical.md`
- 将计划及相关任务规划与上下文写入 `tasks/`: i. `tasks/active_context.md`, ii. `tasks/tasks_plan.md`