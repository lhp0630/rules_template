---
description: LangGraph 开发最佳实践 (代码组织、性能、安全、测试)
globs: **/*.py
alwaysApply: false
---
# LangGraph 最佳实践与编码规范

本文概述 LangGraph 开发的最佳实践与编码规范, 为构建稳健、可维护、可扩展的 LangGraph 应用提供清晰、可执行的指引, 涵盖代码组织、常见模式、性能、安全、测试与常见陷阱。

## 库信息:

- Name: langgraph
- Tags: ai, ml, llm, python, agent-framework, workflow

## 1. 代码组织与结构

### 1.1. 目录结构最佳实践


my_langgraph_project/
├── data/                        # 数据集、知识库或其他数据文件
├── src/                         # 源代码
│   ├── components/              # 可复用组件 (如自定义 node、tool)
│   │   ├── __init__.py
│   │   ├── retrieval.py         # 检索相关 node
│   │   ├── tool_selector.py     # tool 选择逻辑
│   │   └── ...
│   ├── graphs/                  # 图定义
│   │   ├── __init__.py
│   │   ├── customer_support.py  # 示例: 客服图
│   │   ├── rag_pipeline.py      # 示例: RAG 流水线图
│   │   └── ...
│   ├── utils/                   # 工具函数与 helper
│   │   ├── __init__.py
│   │   ├── config.py            # 配置加载
│   │   ├── logging.py           # 日志配置
│   │   └── ...
│   ├── schemas/                 # 数据 schema 与类型定义
│   │   ├── __init__.py
│   │   ├── agent_state.py       # agent state 定义
│   │   └── ...
│   ├── main.py                  # 应用入口
│   └── ...
├── tests/                       # 单元测试与集成测试
│   ├── __init__.py
│   ├── components/              # 自定义组件测试
│   ├── graphs/                  # 图定义测试
│   ├── utils/                   # 工具函数测试
│   └── ...
├── .env                         # 环境变量
├── requirements.txt             # 项目依赖
├── pyproject.toml               # 项目元数据与构建配置
└── README.md                    # 项目文档


### 1.2. 文件命名约定

-   Python 文件: `snake_case.py` (如 `customer_support.py`, `retrieval_node.py`)
-   类名: `PascalCase` (如 `CustomerSupportGraph`, `RetrievalNode`)
-   变量与函数: `snake_case` (如 `user_query`, `process_message`)
-   配置文件: `config.yaml` 或 `config.json`

### 1.3. 模块组织最佳实践

-   将相关功能归入模块 (如 `components`, `graphs`, `utils`)
-   使用 `__init__.py` 将目录变为 package
-   保持模块聚焦, 避免单文件过大
-   模块内使用相对 import, 避免命名冲突

### 1.4. 组件架构建议

-   为常见任务设计可复用组件 (如数据检索、文本摘要、tool 选择)
-   为组件创建抽象基类或接口, 促进复用与模块化
-   使用依赖注入配置组件及其依赖
-   设计组件时遵循单一职责原则 (SRP)

### 1.5. 代码拆分策略

-   将大型图定义拆分为更小、更易管理的文件
-   对非立即需要的组件使用 lazy loading
-   部署时可用 module bundler (如通过插件的 esbuild) 优化 bundle 体积
-   若整体系统规模与复杂度需要, 可拆为微服务, 通过 REST 或消息队列通信

## 2. 常见模式与反模式

### 2.1. 设计模式

-   **State Management Pattern**: 用专用 class 或数据结构封装 agent state, 保证一致性与可维护性; 用 LangGraph 的 `StateGraph` 清晰定义状态转移
-   **Node Pattern**: 为信息检索、tool 选择、响应生成等常见任务定义可复用 node
-   **Conditional Edge Pattern**: 用 conditional edge 基于 agent state 或外部因素实现分支逻辑, 使图更动态、响应更快
-   **Retry Pattern:** 在 node 或 edge 内实现重试, 处理瞬时错误或 API rate limit; 使用指数退避, 避免压垮故障服务
-   **Orchestration Pattern**: 用 LangGraph 编排复杂 agent 工作流, 将具体任务委托给专用组件或服务

### 2.2. 常见任务推荐做法

-   **Information Retrieval**: 用 LangChain retrieval chain 或自定义 node 从外部源获取相关信息
-   **Tool Selection**: 实现 tool 选择 node, 根据用户 query 与 agent state 动态选择合适 tool
-   **Response Generation**: 用 LangChain LLMChain 或自定义 node, 基于检索信息与 agent state 生成响应
-   **Error Handling:** 在 node 与 edge 内实现稳健错误处理, 优雅处理异常并避免崩溃; 记录所有错误并做监控, 便于快速发现与修复

### 2.3. 反模式与代码坏味道

-   **Monolithic Graphs**: 避免 node/edge 过多、过于复杂的图; 拆成更小、可管理的 subgraph
-   **Hardcoded Values**: 避免在图定义中硬编码; 用配置文件或环境变量管理可配置参数
-   **Ignoring Errors**: 始终处理异常并适当记录; 忽略错误会导致不可预期行为与难调试问题
-   **Over-Reliance on Global State**: 尽量减少全局 state, 避免副作用并提升可测试性
-   **Lack of Testing**: 充分测试所有组件与图定义, 确保功能正确并覆盖边界情况
-   **Infinite Loops:** 确保 conditional edge 定义清晰, 避免无限循环

### 2.4. State 管理最佳实践

-   定义清晰、简洁的 agent state schema
-   对 agent state 使用不可变数据结构, 避免意外修改
-   将 agent state 持久化到数据库或其他存储, 支持长对话或长任务; 可考虑 vector database 以高效检索
-   为 agent state schema 做版本管理, 支持 schema 迁移
-   使用 LangGraph checkpointing 保存与恢复 agent state

### 2.5. 错误处理模式

-   在 node 与 edge 内用 try-except 捕获异常
-   记录所有 error/warning 及相应上下文
-   对瞬时错误或 API rate limit 实现重试
-   用 fallback 机制优雅处理不可恢复错误
-   将错误处理逻辑集中到专用模块或 class
-   实现 circuit breaker, 防止级联故障

## 3. 性能考量

### 3.1. 优化技巧

-   **Caching**: 对频繁访问的数据或 LLM 响应做缓存
-   **Batching**: 批量请求外部 API 以降低延迟
-   **Asynchronous Operations**: 用异步 I/O 做非阻塞操作, 提升响应性
-   **Parallel Processing**: 用多线程或多进程并行计算密集型任务
-   **Graph Optimization**: 优化图结构, 减少 node 与 edge 数量
-   **Prompt Optimization**: 精心设计 prompt, 减少 token 并提升 LLM 表现
-   **Reduce LLM calls**: 尽可能缓存 LLM 响应; 针对特定任务微调小模型以降低延迟与成本

### 3.2. 内存管理考量

-   监控内存使用, 发现泄漏或过度占用
-   使用垃圾回收释放未用内存
-   避免在 agent state 中存放大对象
-   对大数据集使用 streaming 或 lazy loading

### 3.3. (不适用, LangGraph 不直接处理渲染)

### 3.4. Bundle 体积优化

-   用 module bundler (如 esbuild) 优化 bundle 体积
-   移除未用代码与依赖
-   用 code splitting 按 route/组件只加载必要代码
-   用 gzip 或 Brotli 压缩 bundle

### 3.5. Lazy Loading 策略

-   对非立即需要的组件使用 lazy loading
-   按需加载大数据集或模型
-   用 code splitting 按图或组件只加载必要代码

## 4. 安全最佳实践

### 4.1. 常见漏洞与防护

-   **Prompt Injection**: 严格校验用户输入并 sanitize prompt, 防止 prompt 注入
-   **Data Leaks**: 静态与传输中加密敏感数据
-   **Unauthorized Access**: 实现强认证与授权, 控制对应用与数据的访问
-   **Denial of Service (DoS)**: 实现 rate limiting 与请求过滤, 防止 DoS
-   **Code Injection**: 避免基于用户输入执行任意代码
-   **API Key Exposure**: 用环境变量或 secrets 管理系统安全存储 API key, 勿提交到版本控制

### 4.2. 输入校验

-   校验所有用户输入, 防止 prompt 注入等漏洞
-   用正则或其他方式确保输入符合预期格式
-   Sanitize 输入, 移除有害字符或代码
-   限制输入长度, 防止 buffer overflow

### 4.3. 认证与授权

-   使用强认证 (如 MFA) 验证用户身份
-   实现 RBAC, 限制对敏感数据与功能的访问
-   安全 session 管理, 防止 session 劫持
-   用 hashing 与 salting 安全存储密码

### 4.4. 数据保护

-   静态与传输中加密敏感数据
-   所有 API 通信使用 HTTPS 等安全协议
-   数据脱敏, 防止未授权访问
-   定期备份, 防止数据丢失
-   遵守 GDPR、CCPA 等隐私法规

### 4.5. 安全 API 通信

-   所有 API 通信使用 HTTPS
-   实现 API 认证与授权
-   校验 API 请求与响应
-   用 rate limiting 防止 API 滥用
-   监控 API 流量, 发现可疑活动

## 5. 测试方法

### 5.1. 单元测试

-   为所有组件与工具函数编写单元测试
-   用 mocking/stubbing 隔离被测组件
-   测试边界情况与错误条件
-   追求高测试覆盖率

### 5.2. 集成测试

-   编写集成测试验证组件间交互
-   测试与外部 API、服务的集成
-   使用接近生产环境的测试环境

### 5.3. 端到端测试

-   编写 E2E 测试验证完整应用流
-   用 Playwright、Selenium 等框架自动化 E2E
-   从用户视角测试应用

### 5.4. 测试组织

-   单元/集成/E2E 测试分目录存放
-   测试文件与函数使用描述性命名
-   遵循一致的测试命名约定
-   用 test suite 分组相关测试

### 5.5. Mocking 与 Stubbing

-   用 mock 替换外部依赖
-   用 stub 替换复杂组件为简化版本
-   用 pytest-mock 等框架简化 mock/stub

## 6. 常见陷阱与注意事项

### 6.1. 常见错误

-   **Incorrect State Management**: agent state 管理不当会导致行为不一致与错误结果
-   **Ignoring Edge Cases**: 未处理边界情况会导致意外错误与崩溃
-   **Over-Engineering**: 图定义过度复杂, 难以理解与维护
-   **Insufficient Testing**: 测试不足会导致未发现的 bug 与故障
-   **Not Handling Asynchronous Operations Correctly:** LangGraph 与 LLM 普遍使用 async; 未 await 会导致不可预期结果

### 6.2. 边界情况

-   **Empty User Inputs**: 处理空或无效用户输入
-   **API Rate Limits**: 实现重试与 rate limiting
-   **Unexpected API Responses**: 处理外部 API 异常响应
-   **Large Data Sets**: 用 streaming 或 lazy loading 处理大数据集

### 6.3. 版本相关问题

-   注意 LangGraph 与 LangChain 不同版本间的兼容性
-   查阅文档与 release notes 中的版本说明
-   固定依赖版本, 避免不可预期行为

### 6.4. 兼容性考量

-   确保 LangGraph 与应用中其他技术兼容
-   测试与外部 API、服务的集成
-   使用一致的库与依赖集合

### 6.5. 调试策略

-   用 logging 跟踪执行流并定位错误
-   用 debugger 单步调试并检查变量
-   用测试框架编写单元/集成测试
-   用监控工具跟踪性能与瓶颈
-   可视化图结构, 理解执行流

## 7. 工具与环境

### 7.1. 推荐工具

-   **IDE**: PyCharm, 带 Python 扩展的 VS Code
-   **Virtual Environment Manager**: venv, conda
-   **Testing Framework**: pytest
-   **Mocking Framework**: pytest-mock
-   **Linting and Formatting**: pylint, black
-   **Module Bundler**: 通过插件的 esbuild
-   **CI/CD**: GitHub Actions, GitLab CI
-   **Secrets Management**: HashiCorp Vault, AWS Secrets Manager, Azure Key Vault
-   **Monitoring**: LangSmith, Prometheus, Grafana

### 7.2. 构建配置

-   用 `poetry` 或 `pip` 管理依赖
-   用 `pyproject.toml` 或 `setup.py` 定义项目元数据与构建配置

### 7.3. Lint 与格式化

-   用 `pylint` 或 `flake8` 校验代码风格并发现潜在错误
-   用 `black` 或 `autopep8` 自动格式化
-   为 linter 与 formatter 配置一致规则

### 7.4. 部署

-   用 Docker 容器化应用
-   部署到 AWS、Azure、Google Cloud 等云平台
-   用 Terraform 或 Ansible 自动化部署
-   采用 blue-green 部署, 最小化 downtime

### 7.5. CI/CD

-   用 GitHub Actions 或 GitLab CI 自动化测试、构建与部署
-   CI/CD pipeline 中运行测试、linter、formatter
-   通过 code review 保证质量与安全

## 结论

遵循上述最佳实践与编码规范, 可构建稳健、可维护、可扩展的 LangGraph 应用, 并便于团队成员在同一 codebase 上协作。
