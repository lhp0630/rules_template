---
description: MCP Server 开发指引模板
globs: 
alwaysApply: false
---
# MCP 插件开发协议

⚠️ CRITICAL: 测试完成前 DO NOT USE attempt_completion ⚠️

## Step 1: 规划 (PLAN MODE)
- 此工具解决什么问题?
- 将使用哪个 API/服务?
- 认证要求是什么?
  □ 标准 API key
  □ OAuth (需要单独的 setup 脚本)
  □ 其他凭证

## Step 2: 实现 (ACT MODE)
1. Bootstrap
   - Web 服务、JavaScript 集成或 Node.js 环境:
     ```bash
     npx @modelcontextprotocol/create-server my-server
     cd my-server
     npm install
     ```
   - 数据科学、ML 工作流或 Python 环境:
     ```bash
     pip install mcp
     # 或使用 uv (推荐)
     uv add "mcp[cli]"
     ```

2. 核心实现
   - 使用 MCP SDK
   - 实现完整日志
     - TypeScript (Web/JS 项目):
       ```typescript
       console.error('[Setup] Initializing server...');
       console.error('[API] Request to endpoint:', endpoint);
       console.error('[Error] Failed with:', error);
       ```
     - Python (数据科学/ML 项目):
       ```python
       import logging
       logging.error('[Setup] Initializing server...')
       logging.error(f'[API] Request to endpoint: {endpoint}')
       logging.error(f'[Error] Failed with: {str(error)}')
       ```
   - 添加类型定义
   - 带上下文处理错误
   - 必要时实现 rate limiting

3. 配置
   - 按需从用户获取凭证
   - 添加到 MCP 设置:
     - TypeScript 项目:
       ```json
       {
         "mcpServers": {
           "my-server": {
             "command": "node",
             "args": ["path/to/build/index.js"],
             "env": {
               "API_KEY": "key"
             },
             "disabled": false,
             "autoApprove": []
           }
         }
       }
       ```
     - Python 项目:
       ```bash
       # 命令行直接安装
       mcp install server.py -v API_KEY=key
       
       # 或在 settings.json 中
       {
         "mcpServers": {
           "my-server": {
             "command": "python",
             "args": ["server.py"],
             "env": {
               "API_KEY": "key"
             },
             "disabled": false,
             "autoApprove": []
           }
         }
       }
       ```

## Step 3: 测试 (BLOCKER ⛔️)

<thinking>
在使用 attempt_completion 之前, MUST 确认:
□ 是否已测试 EVERY tool?
□ 是否已就每项测试获得用户确认?
□ 是否已记录测试结果?

若任一为「否」, MUST NOT 使用 attempt_completion。
</thinking>

1. 逐项测试 Tool (REQUIRED)
   □ 用有效输入测试每个 tool
   □ 验证输出格式正确
   ⚠️ 全部 tool 测试完成前 DO NOT PROCEED

## Step 4: 完成
❗ STOP AND VERIFY:
□ 每个 tool 均已用有效输入测试
□ 每个 tool 的输出格式正确

仅当 ALL tools 测试完成后, 方可使用 attempt_completion。

## 关键要求
- ✓ MUST 使用 MCP SDK
- ✓ MUST 有完整日志
- ✓ MUST 逐项测试每个 tool
- ✓ MUST 优雅处理错误
- ⛔️ NEVER 在完成前跳过测试
